import { Component, EventEmitter, Output } from "@angular/core";

@Component({
  selector:'App-header',
  templateUrl:'./First.component.html'
})
export class firstcomponent {
  //@Output() tabFeature = new EventEmitter<string>();

  //onSelect(tab:string) {
  //  this.tabFeature.emit(tab);
  //}
}
